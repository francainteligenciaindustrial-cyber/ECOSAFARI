node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Raw photo staging folders — not referenced by any code, only public/ is
# actually served. Keeping these out of git avoids ~25MB of unused binaries.
IMAGENS/
IMAGENS VAGALUME/
_backup_originais_vagalume/
images/
vagalume/
assets/
fotos/
fotoss/

.vercel
.claude/

# Internal meeting transcripts/notes accidentally dropped in the repo root —
# not site content, and this one specifically has a spoken-aloud email
# password in it. Never belongs in git history.
*Transcript*.pdf
